<nav class="sf-nav" data-controller="navbar">
  <div class="sf-nav-container">
    <%= link_to root_path, class: "sf-nav-logo" do %>
      <div class="sf-nav-logo-icon">P</div>
      Parse DB
    <% end %>

    <button class="sf-nav-toggler" data-action="click->navbar#toggleMobile">
      <span></span>
      <span></span>
      <span></span>
    </button>

    <div class="sf-nav-links">
      <% if user_signed_in? %>
        <%= link_to "Home", root_path, class: "sf-nav-link" %>
        <%= link_to "Chats", "#", class: "sf-nav-link" %>

        <div class="sf-nav-dropdown" data-navbar-target="dropdown">
          <button class="sf-nav-avatar" data-action="click->navbar#toggleDropdown">
            <%= image_tag "https://kitt.lewagon.com/placeholder/users/ssaunier", alt: "menu" %>
          </button>
          <div class="sf-nav-dropdown-menu">
            <%= link_to "O meu perfil", "#" %>
            <%= link_to "Chats", "#" %>
            <div class="sf-nav-dropdown-divider"></div>
            <%= link_to "Log out", destroy_user_session_path, data: { turbo_method: :delete } %>
          </div>
        </div>
      <% else %>
        <%= link_to "Login", new_user_session_path, class: "sf-nav-cta" %>
      <% end %>
    </div>
  </div>
</nav>
